# Core numeric stack — pinned <2.0 because mediapipe, numba (via librosa/whisper)
# and spacy 3.7 do not support NumPy 2.x
numpy>=1.24.0,<2.0

# App / UI
streamlit>=1.32.0,<2.0

# Computer vision / gaze tracking
opencv-python>=4.9.0,<5.0
mediapipe==0.10.14

# Audio processing
librosa>=0.10.1,<0.11
soundfile>=0.12.1

# Video I/O — API used in code (verbose=/logger= on write_audiofile,
# `from moviepy.editor import VideoFileClip`) is MoviePy 1.x only; do not upgrade to 2.x
moviepy>=1.0.3,<2.0

# Speech-to-text
openai-whisper==20231117
torch>=2.0.0,<2.5

# NLP
spacy>=3.7.2,<3.8
# After installing requirements, download the spaCy English model:
#   python -m spacy download en_core_web_sm

# Data / ML
pandas>=2.0.0,<2.3
scikit-learn>=1.3.0,<1.6
matplotlib>=3.7.0,<3.10
